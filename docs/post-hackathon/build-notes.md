# Post-hackathon build notes

## 2026-09-06 — PH00 baseline and PH01 local ingestion spike

- Starting commit: `dd7169b feat: add controlled proposal decisions` on `main`. The working tree already contained unfinished export/UI and post-hackathon documentation changes; they were preserved. No reset, checkout, or cleanup was performed.
- Runtime baseline: Node `22.16.0`, npm `10.9.2`, Next.js `16.3.3`, TypeScript `5.9.3`, Vitest `4.1.11`. Project-workflow feature flags are not implemented yet; PH03 will add them.
- Baseline checks: `npm run lint`, `npm run typecheck`, and `npm test` passed. Before the ingestion spike, tests were 14 files passed, one file skipped; 82 tests passed and two were skipped. Turbopack compiled and type-checked but then failed with a Node `Out of memory: HashMap::Initialize` error while collecting page data. The installed Next.js guide documents Webpack as a supported build mode; `next build --webpack` completed static generation and standalone tracing. The repository's `build` script now selects that reproducible mode. This is a targeted build-environment mitigation, not a product behavior change.
- PH01 local parser choices: `pdf-parse@2.4.5` (Apache-2.0; Node 22 supported) for page-aware PDF text extraction and `fast-xml-parser@5.11.1` (MIT) for FDX. FDX rejects all DTD/entity declarations before parsing and disables entity processing. PDF parsing disables JavaScript evaluation, uses strict parse errors, and rejects encrypted/unreadable documents. The parser output is source-traceable but is a spike only; no uploads, Cloud Storage, job worker, or public API was added.
- The local ingestion harness passed: FDX produced 7 ordered blocks and two traceable scenes in 8 ms at 86.5 MB RSS; the generated two-page text PDF produced 5 blocks and two page-traceable scenes in 95 ms at 98.8 MB RSS. DTD/entity FDX and non-PDF input were rejected in under 1 ms. Metrics are directional local observations, not release capacity.
- Open evidence: actual Cloud Run measurements, near-limit files, scan/encryption corpus, parser resource limits, and final advertised limits remain open. PH01 must not be checked complete until those measurements are recorded in `feasibility.md`.
- PH01 was extended locally: a mixed text/scan-like PDF leaves a `PAGE_TEXT_MISSING` warning, the 200/201 FDX scene boundary accepted/rejected in 23 ms, and the 150/151 PDF page boundary accepted/rejected in 167 ms at 120.4 MB RSS. The sandbox shell initially did not expose the configured Google Cloud SDK on `PATH`; the SDK is installed at `C:\\Users\\arkad\\AppData\\Local\\Google\\Cloud SDK\\google-cloud-sdk\\bin\\gcloud.cmd`. Its active project is `scriptops-agentic-arkad`. A protected, synthetic-input parser smoke route is implemented and production-built; live deployment and its measurements are deferred to the PH06 release gate. The checklist permits PH02 to rely on the verified local parser contract without claiming PH01 release completion.

## 2026-09-06 — PH02 contract foundation

- Added strict, fixture-independent contracts for projects, planning inputs, product script versions, accepted scene revisions, initial production plans, initial-plan drafts, and project jobs. New plan fields accept an explicit shooting country/region/currency instead of New Mexico/USD literals; FDX source spans may have no page number, and scene display numbers accept `12A`.
- The initial-plan contract requires complete scene/schedule/evidence/reference coverage while allowing an empty casting list. It has no revision budget deltas, requires base plan version zero, and fences job inputs with a 64-character immutable hash. Product records use their own contracts and do not import the hackathon fixture version or golden reference.
- Focused verification passed 12 tests across ingestion and project contracts, plus TypeScript and ESLint. Tests cover strict unknown-field rejection, foreign schedule references, initial budget deltas, missing immutable job input hashes, empty casting, and page-less FDX spans.
- **PH02 complete:** the contract slice establishes the provisional US-NM/USD profile and one-allocation-per-scene schedule model. The former checklist asked for a three-script model experiment before any generation runner existed; that provider-dependent quality/cost evidence now belongs to PH09, which builds the runner. No project repository, API, upload, worker, or provider request was added in PH02.

## 2026-09-06 — PH03 project foundation (local slice)

- Added owner-scoped project persistence with immutable planning-input records under `projects/{projectId}/inputs/{version}`. The runtime repository selects Firestore whenever `GOOGLE_CLOUD_PROJECT` is configured and keeps the demo repository and demo identity paths independent.
- Added Clerk-only real-project actors: project APIs return a configuration error until Clerk and its allow list are set. There is deliberately no development-preview or demo actor for a real project.
- Added `GET`/`POST /api/projects` and `GET`/`PATCH /api/projects/{projectId}`. Creation requires an `Idempotency-Key`, stores only its owner-scoped SHA-256 record key, and returns the original result for an equivalent retry. Reads conceal cross-owner IDs as not found; updates use `recordVersion`; archived projects accept restore only.
- Local verification passed: `npm run verify:post-hackathon:projects`, `npm run typecheck`, focused ESLint, `npm run lint`, `npm test` (100 passed, 2 existing skips), `npm run verify:post-hackathon:ingestion`, `npm run build`, and `git diff --check`. The initial build exposed Zod 4's restriction on deriving an `.omit()` schema from a refined object; planning-input request and persisted schemas now share the same shoot-window invariant without that unsupported derivation.

- **PH03 complete:** the local Firestore emulator at `127.0.0.1:8085` passed `npm run verify:post-hackathon:projects:firestore`. The isolated test created two projects for one owner and one for another, exercised idempotent retry, concealed the foreign project, checked immutable inputs, rejected a stale write and archived write, and restored the archived project. It removed only its unique project documents and request record before the emulator stopped. No live Firestore or Cloud Run service was changed.

## 2026-09-06 — PH04–PH06 in progress

- PH04 project pages, strict real-project actor boundary, empty workspace, feature flag, and creation workflow were added locally. PH05 adds a browser upload control, owner-scoped reservation/finalization APIs, signed V4 writes, and server-side generation/size/type verification. PH06 has persisted job leases, stale-worker fencing, an OIDC worker route, and a per-project job reservation contract; Cloud Tasks outbox/dispatch work remains open.
- Created the private regional bucket `gs://scriptops-screenplays-scriptops-agentic-arkad-us-central1` after explicit approval. It uses uniform bucket-level access, public-access prevention, versioning, the checked-in CORS policy, and a seven-day noncurrent-version lifecycle rule. The only direct application grant is bucket-scoped `roles/storage.objectAdmin` for `scriptops-app@scriptops-agentic-arkad.iam.gserviceaccount.com`. Cloud Run was not redeployed or reconfigured in this step.

## 2026-09-06 — PH07–PH08 scene-review slice

- Persisted parsed scene manifests alongside immutable source blocks and bound uploaded script records to the exact verified Cloud Storage generation. The worker parser writes a `review-ready` script only after saving its manifest; automatic durable dispatch and deployed parse evidence remain PH06/PH07 work.
- The project workspace now resumes an active uploaded script after reload. It presents parsed scenes with their source blocks, version-fenced heading edits, adjacent merge, split, warning acknowledgement, and an acceptance action. Upload finalization updates the active-script pointer in the same Firestore transaction as the script/session record.
- Scene-review drafts preserve parser warning source IDs and predecessor provenance. An excluded `TEXT_OUTSIDE_SCENE` source block must be restored as an ordered scene before acceptance; acknowledgement alone cannot hide that structural gap. Acceptance atomically writes the accepted revision and project pointer. Reopen creates a child draft and clears that pointer atomically; it is forbidden after an approved plan exists. Accepted revisions reject edit requests.
- Focused verification passed: `npm run typecheck`; ESLint on the changed workspace, upload, review, and route files; `npx vitest run tests/unit/project-workspace.test.tsx tests/unit/scene-review.test.ts tests/unit/project-upload-contracts.test.ts tests/integration/script-ingestion.test.ts --reporter=verbose`; and `git diff --check`. The added unit case proves an excluded block blocks acceptance until restoration.
- PH08 is not complete: the review API still needs Firestore-emulator integration coverage and browser verification, and PH06 has not yet dispatched parsing automatically after upload. PH09 remains blocked on those gates.

## 2026-09-06 — PH06 durable parse dispatch slice

- Finalization now transactionally publishes one deterministic `parse-{scriptId}` job and one project-scoped quota reservation after storage verification. It then attempts a uniquely named Cloud Tasks request to the project worker. A dispatch error leaves the verified job queued for reconciliation rather than deleting it or claiming a successful parse.
- The task worker verifies the existing OIDC identity, claims a lease, parses the exact generation-bound object, persists the manifest, and writes `review-ready` only before completing the job. Parse failures requeue at most twice and become terminal on the third failed attempt; a final failure marks the script failed. The former manual parser endpoint uses the same parse implementation.
- Added a Firestore-emulator contract for job publication. It proved that a retry of finalization produces one job and one quota reservation. The emulator first exposed an incorrect attempt to validate lease-bearing stored records with the strict public job schema; persisted lease fields are now explicitly removed before validating the public projection. The isolated test namespace was recursively deleted before the emulator stopped.
- Verification passed: `npm run verify:post-hackathon:jobs:firestore` with `FIRESTORE_EMULATOR_HOST=127.0.0.1:8085`, `npm run typecheck`, focused ESLint, `npm test` (110 passed, 4 emulator-gated skips), and `git diff --check` earlier in the PH08 slice. The separate project queue, its IAM, automatic reconciliation trigger, and deployed worker proof remain open; PH06 is not complete.

- Deployed the validated worker revision after explicit deployment authorization. Cloud Tasks queue `projects/scriptops-agentic-arkad/locations/us-central1/queues/scriptops-projects` is isolated from ripple work and configured with three attempts, a 900-second retry window, concurrency two, and one dispatch per second. Cloud Run revision `scriptops-00038-xjt` serves 100% of traffic and has `PROJECT_TASKS_QUEUE=scriptops-projects`; existing secrets were inspected only as references, never read. This proves configuration and rollout, not an end-to-end user upload/worker completion.

- Added a protected reconciliation route that scans `validating`/`parsing` scripts, republishes missing task delivery, and resets expired leases before redispatch. It was production-built and deployed in Cloud Run revision `scriptops-00039-bwt` (100% traffic). The Cloud Scheduler API was enabled and the OIDC job `scriptops-project-reconcile` was created in `us-central1`, calling the route every five minutes with the existing task-invoker identity. A manual trigger was accepted by Cloud Scheduler; provider delivery status/log evidence was not yet available, so live invocation remains open.

## 2026-09-07 — PH06–PH08 deployed verification

- Live FDX upload initially returned `UPLOAD_STORAGE_UNAVAILABLE`. Redacted route diagnostics identified the signed-URL path as the failing boundary; after the explicitly approved self `roles/iam.serviceAccountTokenCreator` grant to the Cloud Run application identity, the production upload reserved, uploaded, verified, and queued successfully. The `final-draft-basic.fdx` fixture progressed to the persisted `review-ready` state without an initiating-browser request.
- The deployed review flow was exercised on project `PH08 verification screenplay`: heading correction, split, adjacent merge, warning acknowledgement, excluded-block restoration, acceptance, reload, reopen, and reacceptance all persisted. That test revealed a split-then-merge duplicate-heading defect; `scriptops-00041-wbm` fixes equal-heading merges and the review was reopened and reaccepted with the corrected heading. The final reload shows the accepted revision.
- Scheduler delivery was real but initially returned HTTP 500 because Firestore rejected the reconciliation collection-group query. The required field index was created and reached `READY`, but the query still required a collection-group scope. The reconciler now uses a bounded project scan with in-memory script-status filtering, removing that recovery-path index dependency. Cloud Run revision `scriptops-00042-97s` serves all traffic, and a manual Scheduler run recorded `POST 200` for `/api/internal/projects/reconcile` at `2026-09-07T00:21:35Z`.
- A second real project, `PH07 PDF verification screenplay`, uploaded the generated text PDF fixture through the authenticated browser. It was storage-verified, parsed by the durable worker, and reloaded to the persisted `Review ph07-verification.pdf` state. Together with the FDX run, this proves both supported formats traverse the deployed product workflow to correctable review state.
- Focused post-fix verification passed: `npx vitest run tests/unit/scene-review.test.ts`, `npm run typecheck`, repository-wide `npm run lint`, and `git diff --check`. A final `npm test` passed 24 files and 110 tests; five emulator-gated tests were skipped. The source deployments each completed their Cloud Run build successfully before traffic shifted.

## 2026-09-07 — PH09 bounded initial generation (local implementation)

- Continued from `dd7169b` on `main` with the inherited dirty export/dashboard, PH00–PH08 and documentation work preserved. Git inspection used a command-local `safe.directory` exception; no global Git configuration, commit, reset, checkout, deployment, or live application data was changed. The user explicitly chose to keep this pass local and leave paid provider verification pending.
- Replaced `lib/planning/initial-plan.ts`'s placeholder builder with strict assembly of generated artifacts. Added `generation.ts`, `planning-evidence.ts`, `providers.ts`, `limits.ts`, `generation-state.ts`, `generation-firestore.ts`, `dispatch.ts`, and `execute.ts`. The accepted review is explicitly converted to a hash-bound planning snapshot. FDX source IDs (including colon-delimited block IDs) and up to 100 source references per scene are now supported in `schemas.ts`; manifest hashes/provenance and actual document sizes are validated in `initial-plan-manifest.ts`.
- Initial generation runs five-scene breakdown batches, cross-scene role/location normalization, sanitized topic research, schedule, budget, locations, casting and complete-manifest assembly. Missing/foreign source and role references, unknown citations, stale/incomplete evidence, zero-budget placeholders and hard-constraint violations cannot publish a complete draft. Empty casting is legitimate. No demo fixture or fallback is reachable from the runtime planning modules. Provider execution uses the existing direct Gemini SDK style behind a typed interface; the ADK transport detail is amended in `planning-verification.md` while Firestore remains the durable state authority.
- Added owned `POST /api/projects/{projectId}/initial-plans` with UUID idempotency and passive `GET /api/projects/{projectId}/initial-plans/{jobId}`. The existing OIDC task worker branches into planning before its parse claim. Stage output and the next outbox entry commit atomically. Claims and writes check current project lifecycle/epoch/revision/input version/job lock and lease token. Reconciliation handles both expired workers and acknowledged dispatch that never reached a worker, with new delivery generations and a paginated active-job registry. Completed drafts retain the project lock and leave the approved-plan pointer empty.
- Added `components/projects/initial-plan-generation.tsx` and connected it to the existing project workspace/page behind `PROJECT_PLANNING_ENABLED`. It starts generation, polls passively, resumes an active job on reload, stops polling on terminal results, and discloses Gemini/Parallel processing. PH10 still owns draft display, supersession/discard, and Plan v1 approval. The Next.js installed route/authentication guides and React best-practices skill were used for route/React changes.
- Finite provisional controls: three attempts per stage; 90-second leases; one-hour job deadline; 180 KB task prompts; 16,384 output tokens; 700 KiB stored-document limit; 25 cents reserved per attempt, $15/job, $30/day/owner and /project, $100/day/global. Ambiguous calls retain reservations. Explicit positive price ceilings must fit the envelope before new jobs can start; there are no assumed current provider prices. `.env.example` documents the disabled flag and required rates. These are local implementation limits, not measured provider guarantees; see `planning-verification.md` for details.
- Added `verify:post-hackathon:planning` and `verify:post-hackathon:planning:firestore` in `package.json`. New tests cover three synthetic FDX scripts through parsing/accepted review/fake planning, 200-scene fake coverage, empty casting, infeasibility, missing source/evidence, citation rejection, stage retry, lease fencing, provider adapter settings and rate-envelope rejection. The opt-in live harness writes synthetic-only reports for future producer quality review and remains disabled.
- Verification passed: `npm run verify:post-hackathon:planning` (15 passed, three live cases skipped); `npm run verify:post-hackathon:planning:firestore` against `127.0.0.1:8086` (four passed); `npm test` (129 passed, 12 skips: five inherited emulator cases, four new PH09 emulator cases, three opt-in provider cases); an additional focused run of the newly added provider/limits suites (four passed); `npm run lint`; `npm run typecheck`; `npm run build` (Webpack production compilation, type checking, static generation and tracing passed); and `git diff --check`. The final test additions also passed focused ESLint and TypeScript. Emulator tests proved duplicate-start races, one complete manifest without approval, old-token/reopen fencing, lost-task recovery, and atomic daily quota rejection. Each emulator run used a unique `demo-ph09-*` namespace and removed its exact test-owned data; the emulator was stopped afterward.
- Full lint initially found an inherited CommonJS PDF scratch generator under `tmp/`. The generator was preserved, and `eslint.config.mjs` now excludes `tmp/**` alongside other generated/scratch output directories. No dependency versions changed in this PH09 pass.
- PH09 remains unchecked: real three-script generated-plan quality/evidence relevance, near-limit model output/latency/cost measurements, model-specific schema/thinking support, deployed task recovery, and authenticated browser interaction remain pending. Local rendering tests cover entry/loading copy, but do not prove browser interaction. PH10 may build on these local contracts; it must not claim PH09's live gate has passed. Earlier PH01/PH04/PH05 unchecked release evidence is unchanged.

## 2026-09-07 — PH09 post-hardening live recheck

- The user explicitly authorized one bounded paid recheck against the existing Google Cloud and Parallel accounts. Vertex rate limiting ended every synthetic case without an application retry: empty desert reached the budget stage, community rehearsal reached normalization, and clock workshop received HTTP 429 before breakdown. Sanitized synthetic-only reports are retained under `docs/post-hackathon/evidence/`; no project data, Cloud Run revision, Firestore record, feature flag, or deployed configuration was changed.
- Partial generated outputs confirm that exact source quotes survive breakdown/normalization. They also exposed generic tax-incentive sources entering the cost topic and a compliance note describing an insurance threshold as a standard requirement. The planning contract now requires direct wage/rate/agreement evidence for cost records, limits compliance notes to cautious verification actions, adds reconciled cited budget line items, preserves exact scene source facts, deduplicates NM Film aliases, and advances the generation/research policies to `initial-v3`/`project-research-v2`.
- Post-change local verification passed: `npm run verify:post-hackathon:planning` (17 passed, three opt-in live cases skipped), `npm run lint`, and `npm run typecheck`. PH09 remains unchecked because the post-hardening full live drafts were not available for quality review.

## 2026-09-07 — PH10 initial review and Plan v1 approval (local slice)

- Added an authenticated proposal-review read and workspace panel that renders the real immutable candidate manifest: title/logline, schedule, reconciled budget line items, locations, casting, evidence, and assumptions. It presents explicit approve and discard actions and contains no invented before/after comparison.
- Approval transactionally re-reads the owned project, proposal-ready job, and complete candidate manifest. It binds the manifest's job/script/revision/input/hash/base-version identity before creating `plans/v1`, updating the project pointer, and marking the job approved. A duplicate approval returns the existing Plan v1 record. Object storage and provider calls remain outside the transaction.
- Discard leaves the immutable candidate manifest intact, marks the job discarded, and clears the active draft lock. Reopening accepted scenes in the presence of a proposal-ready initial-plan job supersedes that job in the same transaction, preventing a stale proposal from being approved after screenplay input changes.
- Local verification passed: `npm test` (138 passed, 14 emulator/provider cases skipped), `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. The isolated emulator at `127.0.0.1:8086` passed `npm run verify:post-hackathon:planning:firestore`: six scenarios covering concurrent duplicate approval, one installed Plan v1, immutable discard, stale proposal rejection, malformed manifest rejection, recovery and quota fencing. Its unique `demo-ph09-*` namespaces were cleaned by the suite before the emulator stopped.
- PH10 remains open for authenticated-browser review and reload evidence. `PROJECT_PLANNING_ENABLED` remains disabled; enabling it requires the separate PH09 deployment/recovery and authenticated-browser release checks as well.
- Authenticated-browser preflight reached the owned `/projects` workspace with the existing PH08 project in the accepted-scenes state. The deployed service does not expose a proposal because planning remains disabled, so no approval, discard, or scene-reopen mutation was performed. Production browser proof must wait for the separately prepared deployment/recovery verification configuration; this check did not alter the feature flag, Cloud Run revision, or project data.
- The user approved deployment of the PH10 build while retaining the disabled flag. Cloud Run revision `scriptops-00043-8m8` serves 100% of traffic; `/api/health` returned `{"service":"scriptops","status":"healthy"}`. The deployed `PROJECT_PLANNING_ENABLED` value is explicitly `false`. No user-facing planning, provider call, Firestore proposal, or project mutation was made. With that flag false, both plan execution and planning reconciliation deliberately remain unavailable, so live recovery and browser approval evidence still require an isolated enabled verification revision or an explicit temporary enablement decision.
- The user explicitly enabled planning in the existing production service. Revision `scriptops-00044-km2` initially refused a start before any provider call because the required explicit pricing values were missing; `scriptops-00045-xlr` added the verified $0.75/M input, $3.75/M output and $0.005/search ceilings. A first bounded live attempt then correctly failed at research evidence coverage after two checkpoints, creating no manifest or baseline.
- That failure exposed a coverage edge case: one shared Parallel result cap could crowd out a required topic. Planning now issues at most six independent, fixed-vocabulary topic searches and charges their full configured maximum against the existing per-attempt allowance. Focused planning provider/evidence/limit tests, typecheck, lint and diff validation passed before deployment. Revision `scriptops-00046-42h` serves the fix.
- The authorized retry on `PH08 verification screenplay` reached all eight checkpoints and produced immutable manifest `64ed95e7-7870-4f47-8d6e-364696999d2a`. Browser review showed three scenes, two shoot days, a $1,215–$1,549 reconciled budget, three location candidates and one casting brief. The producer action approved Plan v1; a reload and project-list visit showed `Plan v1 approved`. A subsequent browser reopen request returned `SCENE_REVIEW_REOPEN_AFTER_PLAN_FORBIDDEN`, leaving the approved baseline intact. **PH10 is complete.** Planning remains enabled in production by explicit user decision; PH09 deployed recovery remains a separate open release check.

## 2026-09-07 — PH11 project revision ripple (local slice)

- Added a project-only ripple pipeline that reads the current immutable approved plan, accepts one selected project scene and a bounded production-change request, then generates a proposed next plan through the existing Gemini provider interface. It does not import or invoke the demo Ripple fixture engine; the demo path remains explicit and unchanged.
- A ripple proposal preserves the approved screenplay scenes and retained evidence while revising schedule, budget, locations, casting, assumptions and warnings. It validates the project currency and region, derives schedule/budget/location deltas at review time, and stores a complete immutable manifest bound to the exact base plan/version.
- Added owner-scoped start, progress, review, approve, discard and history APIs plus workspace controls for selected-scene revision, proposal comparison and approved plan history. Approval creates `plans/v2+` atomically only if the current project pointer still matches the proposal base. Duplicate approval returns the original installed version; discard preserves the manifest and releases only its lock.
- Project ripple jobs use their own active registry and recovery path for expired leases or acknowledged-but-undelivered tasks. They reuse the protected project worker and outbox transport while remaining distinct from initial planning and the separate demo state repository.
- Local verification passed: `npm run verify:post-hackathon:project-ripple` (two unit cases passed; three emulator transaction cases skipped because no local emulator is running), `npm test` (140 passed, 17 gated skips), `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check`. The emulator suite covers v1 → v2 → v3, discard, idempotent old approval, stale pointer rejection, golden demo input isolation, and a second project left unchanged; it is awaiting an isolated loopback emulator run. PH11 remains open for that emulator proof and deployed authenticated-browser/provider verification.
- The user directed deployment to the existing production environment. The final source deployment is Cloud Run revision `scriptops-00049-j7d`, Ready and serving 100% of `scriptops` traffic. Its public `/api/health` endpoint returned `{"service":"scriptops","status":"healthy"}`, and `PROJECT_PLANNING_ENABLED` remains `true`. The duplicate source-deploy submissions during CLI-runner interruption created earlier ready revisions but no feature-flag or configuration change; the final revision is the only one receiving traffic.

## 2026-09-07 — PH12 approved project PDF export (local slice)

- Added a separate project PDF renderer and authenticated `GET /api/projects/{projectId}/plans/{version}/export` route. It reads only the exact immutable `plans/vN` record after owner validation; drafts, guessed versions, and the isolated demo state cannot be exported through this path.
- Project PDFs include approved version/kind/time, plan summary, revision request and predecessor where applicable, schedule, reconciled budget, locations, casting, evidence links, assumptions/warnings, and immutable manifest/job/script/input/hash provenance. The renderer uses the plan currency and flows content beyond two pages instead of using the demo renderer's fixed two-page structure.
- Added historical-version download links to the project workspace and `verify:post-hackathon:project-export`. Unit verification rendered both Plan v1 and revised Plan v2 plus a 60-scene approved plan. A representative v2 document was rendered to PNGs with Poppler and visually checked for legibility, section flow, citations and provenance.
- The user authorized deployment to the existing production service. Cloud Run revision `scriptops-00050-dqt` is Ready and serving 100% of traffic; `/api/health` returned `{"service":"scriptops","status":"healthy"}`. An anonymous request to the real Plan v1 export URL returned HTTP 401 without PDF content. Authenticated browser download of current and historical plan versions remains the release-evidence check for PH12.

## 2026-09-07 — PH13 deletion and maintenance (local slice)

- Added confirmed `DELETE /api/projects/{projectId}` and owner-only `GET /api/project-deletions/{projectId}`. Deletion requires the current record version and exact title, then records only owner ID, operation ID, epoch, timestamps, and cleanup status in `projectTombstones/{projectId}`. It advances the project write epoch, sets `deleting`, clears active locks, creates a delete job, and dispatches the existing OIDC worker. Archive/restore controls and an explicit delete action are now available in the project workspace.
- Cleanup removes upload sessions, project-scoped planning registries/outbox items, every Cloud Storage generation below the project prefix, and the complete nested Firestore tree before recording a completed tombstone. Parse workers now read the active project and exact epoch in every status/ingestion write transaction, so a worker or proposal that loses its epoch cannot recreate content. Maintenance retries queued/failed deletions, deletes expired unfinalized upload staging, and re-sweeps completed tombstone prefixes because an already-issued signed browser upload URL cannot be revoked.
- The application target is removal from active Firestore and Cloud Storage generations within 24 hours, with maintenance re-sweeps during the seven-day tombstone fence. This is an application-storage target, not a backup guarantee: bucket soft-delete and backup retention have not yet been inspected or measured in production, so PH13 remains open and the UI makes no contrary claim.
- Local verification passed: `npm run verify:post-hackathon:project-cleanup` (one exact epoch/lifecycle/script late-write fence), focused existing project/job tests (five passed; two Firestore-emulator cases skipped because no emulator is running), `npm run typecheck`, `npm run lint`, and `git diff --check`. Live deletion during upload, worker execution, and proposal readiness still needs authenticated production evidence against a uniquely named project; approved-plan preservation through ordinary staging GC also remains unproven.
- The user explicitly approved release of this deletion behavior to the existing production service. Cloud Run revision `scriptops-00051-j5q` is Ready and serves 100% of traffic; `GET /api/health` returned HTTP 200 with `{"service":"scriptops","status":"healthy"}`. This proves rollout health, not the authenticated destructive-flow evidence that PH13 still requires.

## 2026-09-07 — PH14 release-evidence start

- Added `verify:post-hackathon:e2e`, an opt-in production-boundary smoke suite. With the explicit current service URL it passed health, signed-out project/deletion API 401, signed-out workspace redirect, and anonymous deletion rejection. The suite uses a guessed UUID and does not create, mutate, or delete project content.
- Added `release-evidence.md` with the deployed revision, configured operational controls, PH01–PH13 evidence mapping, and a strict authenticated two-user/current-production verification matrix. It records only proven facts: production quality review, current-revision authenticated workflows, deletion-race evidence, soft-delete/backup retention measurement, alert thresholds, narrow-screen/keyboard behavior, and rollback drill remain open. PH14 and private-pilot enablement therefore remain open.
- Local verification passed: `npm run verify:post-hackathon:e2e` against `scriptops-00051-j5q` (four passed), `npm run typecheck`, `npm run lint`, and `git diff --check`.
- Authenticated PH14 evidence reached the real `/projects` workspace rather than the demo. On `PH08 verification screenplay`, live archive/restore returned the project to active state, a v2 ripple proposal showed complete impact data and was explicitly approved, and history then presented two approved versions. A bounded Plan v3 attempt exhausted three 25-cent reservations and stopped as `RIPPLE_GENERATION_FAILED`; no v3 plan was published. The persisted control record proved three attempts/75 cents but not the provider cause because the worker intentionally collapsed errors without a structured log. PH14 therefore stopped at that observability boundary; the next slice adds redacted error metadata before one retry.
- Deployed `scriptops-00052-8hh` with a redacted ripple-worker diagnostic record containing only error class/code/status. It is healthy and has 100% traffic. One bounded v3 retry completed immediately; its proposal showed two shoot days, a +$1,315–$1,729 delta, three locations, and 29 retained evidence records. Explicit approval installed Plan v3. The authenticated history now shows v1 initial plus v2/v3 ripple records and their three owner-scoped PDF URLs. The PDF verification project separately reloaded to its persisted review-ready entry point. Browser automation blocks direct download handling, so it cannot prove file-save behavior; it does not indicate an application export error. The remaining destructive lifecycle check awaits a just-in-time confirmation for a disposable test project.
- The user then explicitly confirmed deletion of disposable project `bd23b18c-f8e6-4c5f-ba24-5ad4aef94abd`. Browser automation could not operate the native `window.prompt`, so `scriptops-00053-4z2` replaces it with an accessible inline exact-title confirmation form; typecheck, lint, workspace/cleanup tests, Cloud Run readiness, and health all passed. The UI entered `deleting`, and the finished worker made the project URL return 404. Scoped production inspection proved the project document and every nested collection, upload/session/outbox/active registry entry, and every object generation under only that project prefix were absent. The only retained record is a content-free completed tombstone at epoch 1. This verifies deployed empty-project deletion; in-flight upload/parser/proposal race coverage remains open.
- Production retention was measured rather than assumed. The private upload bucket enforces public access prevention and uniform bucket-level access, has versioning enabled, deletes non-live versions after seven days, and retains soft-deleted objects for seven days. The five-minute authenticated project reconciler is enabled. Application cleanup therefore removes live objects promptly but cannot honestly claim full Cloud Storage erasure in 24 hours; recovery retention may preserve deleted generations for up to seven days. This resolves the G4 policy-disclosure uncertainty but keeps PH13/PH14 open for in-flight deletion-race evidence and the final pilot review.
